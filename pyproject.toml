[project]
name = "docker-hub-git-actions"
version = "0.1.0"
description = "Static DevOps portfolio landing page served by nginx, with a pytest content test suite."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
